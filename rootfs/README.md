# Root Filesystem Installation files
* makeroot.sh
Assemble all various parts of the root operating system files in to rootfs.tgz
* rootfs.tgz
Extract this file into the root of the Nexus filesystem to install new operating system
